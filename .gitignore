# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Secrets — nikdy necommitovať. Master kópia je na VM v ~/faktero-secrets.env,
# .env sa z nej generuje pri deployi.
.env
.env.*
!.env.example

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Zostavená mobilná appka — vyrába ju `npm run build:mobile`
dist-mobile
dist-jazdy

# Xcode — vyrovnávacia pamäť Swift Package Managera.
# Vzniká pri každom otvorení projektu a do repozitára nepatrí; bez tohto sa
# objavuje ako neznáme súbory a mätie pri `git status`.
ios/App/App.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/*
ios-jazdy/App/App.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/*
# Package.resolved je výnimka — Xcode Cloud stavia s vypnutým riešením balíkov
# a bez tohto zoznamu odmietne začať. Na CI si ho vyrába ci_post_clone.sh sám;
# keby to raz prestalo stačiť, dá sa commitnúť z Macu.
!ios/App/App.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved
!ios-jazdy/App/App.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved
packages/drive-detector/.swiftpm/
**/xcuserdata/
